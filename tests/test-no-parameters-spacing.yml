# Test case for smart spacing - file does NOT start with parameters
trigger:
  branches:
    include:
    - main
    - develop

stages:
- stage: Build

  jobs:
  - job: BuildJob
    steps:
    - task: DotNetCoreCLI@2
      inputs:
        command: build
