# Test case for empty values in list items
parameters:
- name: environments
  type: object
  default: []


stages:
- stage: Deploy
  jobs:
  - job:
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: mySubscription
    - template: /templates/deploy.yaml
