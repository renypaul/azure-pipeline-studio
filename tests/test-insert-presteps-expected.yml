# Expected output after full expansion
resources:
  repositories:
  - repository: self

stages:
- stage: Build

  jobs:
  - job: BuildJob
    steps:
    - checkout: self
    - script: echo "Pre-step 1"
      displayName: 'Pre-step: Pre-step 1'
    - script: echo "Pre-step 2"
      displayName: 'Pre-step: Pre-step 2'
    - script: echo "Building..."
      displayName: Build Step
