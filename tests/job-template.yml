# Job template that accepts preSteps parameter
parameters:
- name: jobName
  type: string
- name: preSteps
  type: stepList
  default: []


jobs:
- job: ${{ parameters.jobName }}

  steps:
  - checkout: self

  - ${{ parameters.preSteps }}

  - script: echo "Building..."
    displayName: Build Step
