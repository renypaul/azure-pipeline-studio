# Test case for duplicate expression keys like ${{ insert }}
parameters:
- name: buildParams
  type: object
  default: {}
- name: testParams
  type: object
  default: {}
- name: language
  type: string


stages:
- stage: Build
  jobs:
  - template: /templates/build.yaml
    parameters:
      ${{ insert }}: ${{ parameters.buildParams }}
      ${{ insert }}: ${{ parameters.testParams }}
      language: ${{ parameters.language }}
