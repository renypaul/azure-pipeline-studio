variables:
- ${{ if eq(parameters.environment, 'prod') }}:
  - name: deploymentSlot
    value: production

- ${{ else }}:
  - name: deploymentSlot
    value: staging

- ${{ if eq(parameters.runTests, true) }}:
  - script: |
      npm run test:unit
      npm run test:integration
